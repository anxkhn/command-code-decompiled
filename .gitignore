# The full beautified bundle is 2.2MB / 70K lines — too large for git
# Key sections are extracted into source/ instead
index.mjs

# Original VSIX extraction (cleaned version in vsix-extension/)
vsix-decompiled/

# Node / OS
node_modules/
.DS_Store
*.vsix